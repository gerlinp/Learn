const person = {
  name: 'Gerlin',
  city: 'Boston',
  age: 37,
  isStudent: true,
  skills: ['JavaScript', 'HTML', 'CSS'],
}

const message = `Hi, I'm a ${person.name}. I live in ${person.city}.`
console.log(message)
